// Deprecated — template-for-sale messaging removed.
// File retained as a no-op stub so any stale imports don't break.
// Safe to delete from disk in a follow-up cleanup.
export function TemplateForSale() {
  return null;
}
